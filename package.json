{
  "name": "parcel_test",
  "version": "1.0.0",
  "description": "",
  "source": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && parcel build --dist-dir build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "parcel": "^2.12.0",
    "rimraf": "^5.0.0"
  },
  "devDependencies": {
    "buffer": "^6.0.3"
  }
}